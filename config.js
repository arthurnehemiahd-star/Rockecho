// Where the backend API lives. Change this when the backend runs on a
// different host/port than the default (e.g. once it's deployed somewhere
// real), then just refresh the page — nothing else in the frontend needs to
// change.
window.ROCKECHO_API_BASE = 'http://localhost:3000';
